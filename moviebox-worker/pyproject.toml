[project]
name = "moviebox-api-worker"
version = "0.1.0"
dependencies = [
    "fastapi",
    "pydantic>=2.0",
    "httpx",
    "beautifulsoup4",
    "throttlebuster"
]

[tool.hatch.metadata]
allow-direct-references = true
